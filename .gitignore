node_modules
client/public/output/
deepzoom.py

__pycache__
.venv
db.sqlite3
.secret_key
server/unrest
server/dist
server/main/settings/local.py
.static
.media

_backups
_cache

server/smur/natureRando
# uwsgi request log — generated, rotated by /etc/logrotate.d/uwsgi-projects.
# logs/.gitkeep is tracked so the directory exists in a fresh clone (the systemd
# unit also mkdir -p's it, but a checkout should look right before first start).
logs/*.log
